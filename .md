### flask构建
```
docker build -t flaskapp:test .
docker run  -d --name flaskapp-container  -p 5000:5000  -v "$(PWD)/logs:/var/log"  -v "$(PWD)/results:/app/application/services/dccl_simulation/results" flaskapp:test 
```

### vue构建
```
docker build -t vueapp:test .
docker run -d --name vueapp-container -p 8080:80 vueapp:test
```